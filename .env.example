# Business Cost Tracker — Environment Variables
# Copy to .env and fill in values

# API base URL for your backend (optional).
# When set, the app fetches and saves costs to your API instead of localStorage only.
# Example: https://api.example.com or http://localhost:4000
VITE_API_BASE_URL=
